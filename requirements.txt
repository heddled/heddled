# Heddled's entire third-party surface. Everything else is stdlib.
flask>=2.0
requests>=2.28
PyYAML>=6.0
# Comment-preserving round-trip for console authoring (decision 9).
ruamel.yaml>=0.18
# zstd for stored context (decision 4); stdlib from Python 3.14.
zstandard>=0.22; python_version < '3.14'
